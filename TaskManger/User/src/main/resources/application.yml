server:
  port: 8081

spring:
  application:
    name: User

  datasource:
    url: jdbc:mysql://localhost:3306/task_manger
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true

